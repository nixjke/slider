const defaultOptions = {
  min: 0,
  max: 100,
  step: 2,
  values: [20],
  direction: 'horizontal',
  type: 'single',
  bar: true,
  tip: true,
  scale: true,
}

export default defaultOptions
